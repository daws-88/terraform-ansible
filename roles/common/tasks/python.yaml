- name: install python package
  ansible.builtin.dnf:
    name: "{{ item }}"
    state: present
  loop:
  - python3
  - gcc
  - python3-devel

- name: Install packages based on pip3
  ansible.builtin.pip:
    requirements: requirements.txt
    executable: pip3
  args:
    chdir: /app
